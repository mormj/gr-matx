# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-matx
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND matx_sources
)

set(matx_sources "${matx_sources}" PARENT_SCOPE)
if(NOT matx_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT matx_sources)

add_library(gnuradio-matx SHARED ${matx_sources})
target_link_libraries(gnuradio-matx gnuradio::gnuradio-runtime)
target_include_directories(gnuradio-matx
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-matx PROPERTIES DEFINE_SYMBOL "gnuradio_matx_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-matx PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-matx)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_matx_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-matx)

if(NOT test_matx_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_matx_sources)

foreach(qa_file ${test_matx_sources})
    GR_ADD_CPP_TEST("matx_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
